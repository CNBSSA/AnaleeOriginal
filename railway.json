{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "gunicorn wsgi:app --bind 0.0.0.0:$PORT --worker-class gthread --workers 2 --threads 4 --timeout 600"
  }
}
